Source: neovim
Section: editors
Priority: optional
Maintainer: dcampos <dcampos@users.noreply.github.com>
Build-Depends:
     ninja-build,
     gettext,
     libtool,
     libtool-bin,
     autoconf,
     automake,
     cmake,
     g++,
     pkg-config,
     unzip,
     curl,
     doxygen,
Standards-Version: 4.6.2
Homepage: https://neovim.io/
Rules-Requires-Root: no

Package: neovim
Provides:
    editor,
    vim
Conflicts:
   neovim-runtime
Replaces:
   neovim-runtime
Architecture: any
Depends: ${misc:Depends}, ${shlibs:Depends}
Description: Vim-fork focused on extensibility and usability.
  Neovim is a fork of Vim focused on modern code and features, rather than
  running in legacy environments.
