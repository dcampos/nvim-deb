#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

# These are needed for repeatable builds.
# export SOURCE_DATE_EPOCH := $(shell date -d "$$(dpkg-parsechangelog -SDate)" +%s 2>/dev/null || date +%s)
# export DEB_BUILD_DATE := $(shell date -u -d "@${SOURCE_DATE_EPOCH}" +%Y-%m-%d-%H:%M:%S)
# export SOURCE_DATE_EPOCH DEB_BUILD_DATE

%:
	dh $@

override_dh_auto_clean:
	make distclean

override_dh_auto_build:
	make CMAKE_INSTALL_PREFIX=/opt/nvim CMAKE_BUILD_TYPE=Release

override_dh_auto_test: ;

# override_dh_auto_install:
# 	make install
